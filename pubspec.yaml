name: ybey_app
description: A Flutter app for YBEY - You Become Yourself

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # 🔥 Firebase
  firebase_core: ^2.27.0
  firebase_auth: ^4.17.0
  cloud_firestore: ^4.15.0

  # 🌐 API & State
  http: ^0.13.6
  provider: ^6.0.5
  url_launcher: ^6.1.10

  # 🔊 Audio & Voice
  flutter_tts: ^4.0.0
  audioplayers: ^3.0.1

  # 🍎 iOS style icons
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/audio/
